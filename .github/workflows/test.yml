name: Demo Workflow   # Workflow name

on: push

jobs:
  build:              # Job name
    runs-on: self-hosted
    

    steps:
      - uses: actions/checkout@v3   # Step 1: Checkout code
      - run: sudo mkdir runnertest && cd runnertest && touch test.yml
